name: metam-services

services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-metam}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-metam}
      POSTGRES_DB: ${POSTGRES_DB:-metam}
    ports:
      - "5432:5432"
    volumes:
      - metam_postgres_data:/var/lib/postgresql/data
    networks:
      - metam-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-metam} -d ${POSTGRES_DB:-metam}"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    restart: unless-stopped
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - metam-network

  pgadmin:
    image: dpage/pgadmin4:8
    restart: unless-stopped
    environment:
      PGADMIN_DEFAULT_EMAIL: ${PGADMIN_DEFAULT_EMAIL:-admin@metam.localhost.com}
      PGADMIN_DEFAULT_PASSWORD: ${PGADMIN_DEFAULT_PASSWORD:-ChangeMe123!}
    ports:
      - "5050:80"
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      - metam_pgadmin_data:/var/lib/pgadmin
    networks:
      - metam-network

  platform-api:
    image: vinodreddy1999/metam-services-fullstack:latest
    build: .
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000
    restart: unless-stopped
    environment:
      DATABASE_URL: postgresql+psycopg2://${POSTGRES_USER:-metam}:${POSTGRES_PASSWORD:-metam}@postgres:5432/${POSTGRES_DB:-metam}
      LOG_LEVEL: ${LOG_LEVEL:-INFO}
      ENABLE_PUBLIC_DEMO: ${ENABLE_PUBLIC_DEMO:-true}
      PUBLIC_DEMO_TOKEN_MINUTES: ${PUBLIC_DEMO_TOKEN_MINUTES:-120}
    ports:
      - "8000:8000"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/ready').read()"]
      interval: 15s
      timeout: 5s
      retries: 5
    networks:
      - metam-network

  fullstack-app:
    image: vinodreddy1999/metam-services-fullstack:latest
    build: .
    restart: unless-stopped
    environment:
      DATABASE_URL: postgresql+psycopg2://${POSTGRES_USER:-metam}:${POSTGRES_PASSWORD:-metam}@postgres:5432/${POSTGRES_DB:-metam}
      LOG_LEVEL: ${LOG_LEVEL:-INFO}
      ENABLE_PUBLIC_DEMO: ${ENABLE_PUBLIC_DEMO:-true}
      PUBLIC_DEMO_TOKEN_MINUTES: ${PUBLIC_DEMO_TOKEN_MINUTES:-120}
    ports:
      - "8080:8080"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      platform-api:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8080/ready').read()"]
      interval: 15s
      timeout: 5s
      retries: 5
    networks:
      - metam-network

  frontend:
    image: vinodreddy1999/metam-services-frontend:latest
    build:
      context: ./frontend
      args:
        VITE_API_BASE_URL: http://localhost:8000
    restart: unless-stopped
    ports:
      - "8081:8080"
    depends_on:
      platform-api:
        condition: service_healthy
    networks:
      - metam-network

  worker:
    image: vinodreddy1999/metam-services-fullstack:latest
    build: .
    command: celery -A app.jobs.celery_app worker --loglevel=info
    restart: unless-stopped
    environment:
      DATABASE_URL: postgresql+psycopg2://${POSTGRES_USER:-metam}:${POSTGRES_PASSWORD:-metam}@postgres:5432/${POSTGRES_DB:-metam}
      CELERY_BROKER_URL: redis://redis:6379/0
      CELERY_RESULT_BACKEND: redis://redis:6379/1
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - metam-network

  seed-enterprise-data:
    image: vinodreddy1999/metam-services-fullstack:latest
    build: .
    command: python scripts/seed_enterprise_data.py
    restart: "no"
    environment:
      BASE_URL: http://fullstack-app:8080
      DATABASE_URL: postgresql+psycopg2://${POSTGRES_USER:-metam}:${POSTGRES_PASSWORD:-metam}@postgres:5432/${POSTGRES_DB:-metam}
    depends_on:
      fullstack-app:
        condition: service_healthy
    networks:
      - metam-network

  vinod-live-data-stream:
    image: vinodreddy1999/metam-services-fullstack:latest
    build: .
    command: python scripts/run_vinod_live_stream.py
    restart: unless-stopped
    environment:
      PYTHONPATH: /app
      DATABASE_URL: postgresql+psycopg2://${POSTGRES_USER:-metam}:${POSTGRES_PASSWORD:-metam}@postgres:5432/${POSTGRES_DB:-metam}
      VINOD_LIVE_DURATION_HOURS: ${VINOD_LIVE_DURATION_HOURS:-48}
      VINOD_LIVE_INTERVAL_SECONDS: ${VINOD_LIVE_INTERVAL_SECONDS:-10}
      VINOD_LIVE_BATCH_SIZE: ${VINOD_LIVE_BATCH_SIZE:-50}
      VINOD_LIVE_AUDIT_EVERY_BATCHES: ${VINOD_LIVE_AUDIT_EVERY_BATCHES:-12}
      VINOD_LIVE_HOLD_AFTER_COMPLETE: ${VINOD_LIVE_HOLD_AFTER_COMPLETE:-true}
    depends_on:
      platform-api:
        condition: service_healthy
    networks:
      - metam-network

  nginx:
    image: nginx:1.27-alpine
    restart: unless-stopped
    ports:
      - "80:80"
    volumes:
      - ./deploy/nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      fullstack-app:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://127.0.0.1/health"]
      interval: 15s
      timeout: 5s
      retries: 5
    networks:
      - metam-network

volumes:
  metam_postgres_data:
  metam_pgadmin_data:

networks:
  metam-network:
    driver: bridge
