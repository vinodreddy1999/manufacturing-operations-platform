# Seed Data Plan

The seed creates one demo tenant, admin user, role permissions, feature flags, company, plant, warehouse location, inventory item, balance, and supplier.

Run:

```powershell
npm run prisma:migrate
npm run prisma:seed
```
